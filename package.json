{
  "name": "numan-simple-calculator-at",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:headed": "npx playwright test --headed",
    "test:buildenv": "node ./scripts/run-tests.js",
    "test:build": "node ./scripts/run-tests.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.1",
    "http-server": "^14.1.1"
  }
}
